# -*- coding: utf-8 -*-
"""
拍卖之王 - 屏幕布局常量（1280×720）

这些区域由游戏 UI 决定，是固定布局，不是用户可调参数，
因此不走 strategy_param / custom_action_param，避免被前端覆盖时丢失。

新增出价策略若需要识别同一区域，直接 import 复用，不要再往 JSON 里塞。
若将来支持多分辨率，可把本模块拆成 layout/ 目录按分辨率分文件。
"""

# ============ 顶栏 ============

# 系统估价区域（当轮情报披露后的最低估价）
PRICE_ROI = (1077, 100, 161, 23)

# ============ 出价面板 ============

# 出价输入框
INPUT_ROI = (759, 505, 244, 30)

## 面板内"上轮出价"按钮，需先点击出价
LAST_BID_BTN = (603, 532, 102, 52)

## 输入框清除按钮
CLEAR_BTN = (608, 615, 88, 60)

# 数字按钮
DIGIT_BUTTONS = {
    "1": (263, 360, 88, 60),
    "2": (379, 360, 88, 60),
    "3": (494, 360, 88, 60),
    "4": (263, 445, 88, 60),
    "5": (379, 445, 88, 60),
    "6": (494, 445, 88, 60),
    "7": (263, 530, 88, 60),
    "8": (379, 530, 88, 60),
    "9": (494, 530, 88, 60),
    "0": (263, 615, 88, 60),
}
# ============ 稳定判定 ============

# 稳定判定区域：只框"信息披露卡片"这一列。
# 每轮流程是：新情报先展示进这块区域 -> （可能）更新包裹内物品 -> 更新顶栏估价，
# 所以这块区域静止 = 新一轮的情报已经展示完，再等一小段就能读到新估价。
#
# 注意不要再把这片区域放大到包含包裹区：包裹发现物品轮廓/品质时，
# 会有一圈光沿轮廓移动，导致画面永远"不稳定"（实测会一直撞 12 秒超时）。
SETTLE_ROI = (412, 163, 421, 431)
